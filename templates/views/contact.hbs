<div class="container">
	<h1>报名/联系我们</h1>
</div>
<div class="container">
	<div class="row">
		<div class="col-sm-8 col-md-6">
			{{#if enquirySubmitted}}
			<h4>感谢您的参与:)<br/> 请在微信公众号中搜索“太极家园” 关注我们的公众号。</h4>
			{{else}}
			<form method="post">
				<input type="hidden" name="action" value="contact">
				<div class="form-group {{#if validationErrors.name}}has-error{{/if}}">
					<label>姓名</label>
					<input type="text" name="name.full"  class="form-control" value="{{formData.[name.full]}}">
				</div>
				<div class="form-group {{#if validationErrors.email}}has-error{{/if}}">
					<label>Email/邮箱地址/微信号</label>
					<input type="email" name="email" class="form-control" value="{{formData.email}}">
				</div>
				<div class="form-group {{#if validationErrors.phone}}has-error{{/if}}">
					<label>手机号</label>
					<input type="text" name="phone"  class="form-control" value="{{formData.phone}}">
				</div>
				<!--<div class="form-group {{#if validationErrors.enquiryType}}has-error{{/if}}">
					<label>What are you contacting us about?</label>
					<select name="enquiryType" class="form-control">
						<option value="">(select one)</option>
						{{#each enquiryTypes}}
						<option value="{{value}}">{{label}}</option>
						{{/each}}
					</select>
				</div>-->
				<div class="form-group {{#if validationErrors.message}}has-error{{/if}} ">
					<label>对太极拳的感触</label>
					<textarea name="message" placeholder="Leave us a message..." rows="4" class="form-control">{{formData.message}}</textarea>
				</div>
                                <div class="form-group {{#if validationErrors.gender}}has-error{{/if}}">
                                        <label>性别</label>
                                        <input type="text" name="gender" class="form-control" value="{{formData.gender}}">
                                </div>
				<div class="form-actions">
					<button type="submit" class="btn btn-primary">确认</button>
				</div>
			</form>
			{{/if}}
		</div>
	</div>
</div>
</div>
